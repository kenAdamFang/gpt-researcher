# 精炼版LLM筛选与摘要策略

本报告详细阐述了根据您的最新需求优化后的、用于构建高质量报告上下文的全新工作流。

---

## 0. 前置分析：子查询在新流程中是否产生？

**结论：不产生。**

**分析依据**:
我们设想的新功能将通过一个新的报告源类型 `"local_precise"` 来触发。在 `ResearchConductor` 的主流程 `conduct_research` 方法中，我们将为此类型创建一个新的 `elif` 分支。

```python
# gpt_researcher/skills/researcher.py (设想的修改)
class ResearchConductor:
    async def conduct_research(self):
        # ...
        elif self.researcher.report_source == "local_precise":
            # 我们的新流程代码将在这里
            # (调用筛选、摘要等模块)
            # ...
            # 关键点：这里没有调用 self.plan_research()
```

框架中，只有 `plan_research()` 方法负责调用 `generate_sub_queries` 来生成子查询。由于我们的新分支完全绕过了 `plan_research()`，因此**子查询生成的步骤被彻底跳过**。

所有后续的筛选和摘要步骤，都将直接使用用户输入的**主查询** (`self.researcher.query`) 作为唯一的核心判断依据，保证了研究焦点的高度统一和可控性。

---

## 1. 最终方案：单阶段筛选与深度摘要工作流

本工作流通过一个高效的筛选阶段和一个高质量的摘要阶段，形成最终的报告上下文。

### **第一阶段：单阶段批量相关性筛选**

*   **核心目标**: 从 N 篇总文档中，通过一次批量处理，直接筛选出 Top-K 篇最相关的完整文档。
*   **执行者**: `FAST_LLM` (`Qwen3-32B`)

*   **详细步骤**:
    1.  **加载文档**: `DocumentLoader` 加载全部 N 篇文档。
    2.  **生成预览**: 遍历所有文档，为每一篇提取**前 5000 个词**作为预览内容。
    3.  **动态分批**: 无论 N 为何值（如96, 83, 64），都按每批10个预览进行打包。例如，83篇文档将被分为9个批次（8个10篇，1个3篇）。
    4.  **并发处理所有批次**:
        a.  **构建增强 Prompt**: 为每个批次构建一个包含清晰指令、格式要求和正误示例的“批量评审”Prompt，以最大化地引导 `FAST_LLM` 输出规范的JSON。
        b.  **调用 FAST_LLM**: 将 Prompt 发送给 `FAST_LLM`。
        c.  **健壮的响应解析**: 收到响应后，调用一个专门的 `extract_json_from_response` 函数。此函数使用正则表达式 `re.search(r'\[.*?\]', response)` 从可能混杂了其他文本的响应中，安全地提取出 `[...]` 格式的 JSON 字符串，并进行解析。即使LLM返回 `好的，这是列表: ["a.pdf"]`，也能成功提取 `["a.pdf"]`。解析失败则返回空列表。
    5.  **汇总与筛选**: 将所有批次成功解析出的相关文件名汇总，并据此从原始文档列表中筛选出对应的**完整文档对象**，作为本阶段的最终输出。

### **第二阶段：逐篇深度摘要与结构化提取**

*   **核心目标**: 对第一阶段筛选出的 K 篇高质量文档，进行逐一的、深入的阅读理解，并提取出结构化的、包含精确来源的核心信息。
*   **执行者**: `STRATEGIC_LLM` (`DeepSeek-V3-0324`)

*   **详细步骤**:
    1.  **并发处理筛选出的文档**:
        a.  **构建摘要 Prompt**: 为每一篇文档，将其**全文内容**和**文件名**填充到一个专门的“单篇摘要”Prompt 中，该 Prompt 指示 LLM 必须以包含 `source_document`, `summary`, `key_data` 字段的 JSON 格式返回结果。
        b.  **调用 STRATEGIC_LLM**: 将 Prompt 发送给 `STRATEGIC_LLM` API。
        c.  **处理 `<think>` 标签**: 收到 `STRATEGIC_LLM` 的原始响应文本后，立即调用一个通用的 `extract_final_content` 工具函数。该函数会检查并剥离掉所有在 `</think>` 标签之前的内容，只返回干净的、可能是JSON的字符串。
        d.  **解析 JSON**: 使用 `json_repair.loads()` 将上一步得到的干净字符串解析为结构化的 Python 字典。
    2.  **输出**: 产出一个包含 K 个结构化摘要信息对象的列表。

### **第三阶段：最终上下文整合**

*   **核心目标**: 将第二阶段产出的结构化信息，格式化为对最终报告生成器友好的、人类可读的 Markdown 字符串。
*   **执行者**: 一个简单的格式化函数。

*   **详细步骤**:
    1.  **输入**: 包含 K 个摘要对象的列表。
    2.  **格式化**: 遍历列表，将每个对象的 `source_document`, `summary`, `key_data` 转换成一个格式清晰的 Markdown 文本块，并使用水平分割线 `---` 将不同文档的信息块隔开。
    3.  **输出**: 产出一个名为 `final_context` 的、巨大的 Markdown 字符串，作为最终提供给报告生成模块的全部上下文。

---

## 2. 总结

这个经过您反馈和优化的最终工作流，具备以下特点：

*   **目标明确**: 完全使用主查询，避免了子查询带来的焦点漂移。
*   **流程高效**: 将筛选过程整合为单阶段，减少了不必要的步骤。
*   **过程健壮**: 充分考虑了文档数量不固定、LLM输出不规范等现实问题，并设计了相应的鲁棒处理机制。
*   **分工合理**: 为不同复杂度的任务分配合适的 LLM，并设计了专门的工具来处理不同模型的输出特性，实现了成本和质量的平衡。

该方案为一个高质量、高可信度的自动化报告生成流程奠定了坚实的基础。
