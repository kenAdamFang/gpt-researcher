# LLM 原生筛选策略：一种全新的文档相关性评审方法

## 第一部分：引言与方向调整

### 1.1 问题回顾
在最初的“精确报告”方案中，我们采用了基于 Embedding 的向量相似度搜索来从大量文档中筛选相关内容。该方案虽然高效，但存在一个核心缺陷：它将所有文档内容无差别地切片处理，破坏了单篇文档内部的逻辑和上下文完整性，可能导致对文档核心思想的误判，并为后续的来源精确归因带来困难。

### 1.2 新方向
为了追求更高的筛选准确性、语义理解深度和过程可解释性，我们提出对核心思路进行重大调整：**放弃基于 Embedding 的筛选方案，转而采用一种完全由大语言模型（LLM）驱动的、更接近人类专家评审的筛选策略。**

### 1.3 核心理念
新策略的核心是 **“LLM 驱动的两阶段相关性评审”**。我们不再依赖向量空间中的数学距离来衡量相关性，而是将 `FAST_LLM` 的角色从一个简单的“元信息总结者”提升为核心的“**相关性评审员**”。它将通过直接“阅读”和“判断”来完成筛选任务。

---

## 第二部分：新策略工作流程详解

### 2.1 总览图

```
[80篇本地文档]
      |
      ▼
[阶段一：快速批量初筛] --> (使用 FAST_LLM 对文档预览进行批量判断)
      |
      ▼
[~15-20篇候选文档]
      |
      ▼
[阶段二：单篇深度终审] --> (使用 FAST_LLM 对候选文档全文进行逐一打分)
      |
      ▼
[Top 5-10篇最终文档]  --> (进入后续的“单篇摘要提取”流程)
```

### 2.2 阶段一：快速批量初筛 (Rapid Batch Pre-screening)

*   **目标**: 高效地从80篇文档中过滤掉大部分明显不相关的内容，得出一个规模可控的候选集，同时最大化地减少 LLM API 的调用次数。

*   **流程步骤**:
    1.  **生成文档预览**: 遍历全部80篇文档，为每一篇生成一个简短的文本预览。最简单的实现是 `文件名 + 文档前1000个字符`。
    2.  **分批打包**: 将80个文档预览分成8个批次，每批包含10个预览。
    3.  **设计“批量评审”Prompt**: 为每个批次构建如下的Prompt：
        ```
        你是一个高效的文档分类助理。你的任务是根据主要研究主题，判断一列文档预览的相关性。

        主要研究主题: "{用户查询}"

        以下是10篇文档的预览，以其文件名作为唯一标识符：

        1.  **{文件名_1}**: {预览内容_1}
        2.  **{文件名_2}**: {预览内容_2}
        ...
        10. **{文件名_10}**: {预览内容_10}

        请仔细阅读每一条预览，并找出来与主要研究主题 **直接相关** 的所有文档。

        **输出要求**:
        请严格按照以下 JSON 格式返回一个列表，其中只包含你认为相关的文档的文件名。如果列表中没有相关的，请返回一个空列表。

        ["{相关文件名_A}", "{相关文件名_B}", ...]
        ```
    4.  **执行评审**: 对8个批次，分别调用 `FAST_LLM` 执行一次。
    5.  **收集结果**: 将8次调用返回的所有相关文件名汇总，形成一个候选文档集（例如，18篇）。

### 2.3 阶段二：单篇深度终审 (In-depth Single-Pass Final Review)

*   **目标**: 对第一阶段筛选出的候选集进行逐一的、更深入的评审，并给出量化的相关性分数，以便进行精确排序和筛选。

*   **流程步骤**:
    1.  **输入**: 候选集中的18篇完整文档对象。
    2.  **设计“深度评审”Prompt**: 遍历这18篇文档，为每一篇构建如下的Prompt：
        ```
        你是一位严谨的研究分析师。你的任务是基于主要研究主题，对下面这篇完整的文档进行相关性评估。

        主要研究主题: "{用户查询}"

        文档来源: "{文件名}"
        文档全文 (或为控制成本，可截取前15000字符):
        ---
        {文档全文内容}
        ---

        **评估指令**:
        请基于文档内容与研究主题的匹配度，从 0 (完全不相关) 到 10 (核心相关) 的范围内，给出一个整数相关性分数。

        **输出要求**:
        请严格按照以下 JSON 格式返回结果，不要包含任何其他文字。

        {
          "relevance_score": <你的打分>
        }
        ```
    3.  **执行评审**: 对18篇候选文档，分别调用 `FAST_LLM` 执行一次。
    4.  **排序与筛选**: 收集所有18次调用返回的相关性分数，根据分数从高到低排序，选取分数最高的 Top-N 篇（例如 N=5 或 10）作为最终结果。

---

## 第三部分：利弊分析

### 3.1 优势 (Pros)
*   **语义理解更深**: LLM能理解复杂的上下文和逻辑关系，判断可能比单纯的向量距离更准。
*   **可解释性极强**: 可要求LLM在打分的同时提供“打分理由”，让筛选过程透明化。
*   **筛选标准灵活**: 通过修改Prompt，可以轻松实现“优先选择案例研究”、“排除综述文章”等复杂筛选逻辑。
*   **技术栈简化**: 不再需要依赖单独的Embedding模型。

### 3.2 挑战与劣势 (Cons & Challenges)
*   **成本与延迟**: LLM的API调用成本和网络延迟远高于本地的Embedding计算。对80篇文档完成两阶段筛选，总调用次数可能在几十次，耗时可能达到分钟级。
*   **结果稳定性**: LLM的输出存在一定随机性，不如Embedding模型的结果确定和可复现。
*   **依赖Prompt质量**: 筛选效果高度依赖Prompt的设计，需要精心的“Prompt工程”来保证稳定性和准确性。

---

## 第四部分：与现有流程的衔接

本新策略旨在**完全替换**原有的 `DocumentFilter` 模块。

*   **输出兼容性**: 新策略的最终输出是一个包含 Top-N 篇**完整文档对象**的列表。
*   **无缝集成**: 这个输出格式与我们之前设计的“阶段二：单篇文档摘要与结构化信息提取” (`summarize_single_document` 函数) 的输入要求完全一致。因此，一旦新筛选策略实现，它可以无缝地接入到整个“精确报告”工作流中，后续流程无需任何改动。

---

## 第五部分：结论

“LLM原生筛选策略”是一种更智能、更强大、更灵活的文档筛选方法。它虽然在成本和延迟上高于Embedding方案，但其在理解深度和可解释性上的巨大优势，使其成为生成高质量、高可信度研究报告的理想选择，尤其适合于对报告质量要求极高，且对处理时间有一定容忍度的应用场景。
