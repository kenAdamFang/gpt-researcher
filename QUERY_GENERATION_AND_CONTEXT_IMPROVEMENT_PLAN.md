# 查询生成机制解析与上下文处理改进方案

本报告旨在回答您关于查询生成和上下文处理的疑问，并为您提出的问题提供一套详细的改进方案。

---

## 第一部分：查询生成（子查询）机制解析

### 1. 使用哪个 LLM 生成子查询？

**答案：** 框架会**优先使用 `STRATEGIC_LLM`**，如果失败则**回退到 `SMART_LLM`**。

**代码依据：**
在 `gpt_researcher/actions/query_processing.py` 的 `generate_sub_queries` 函数中，调用 `create_chat_completion` 时明确指定了模型：

```python
# 优先尝试 STRATEGIC_LLM
response = await create_chat_completion(
    model=cfg.strategic_llm_model,
    # ...
)
```

如果上述代码块出现异常，`except` 块会执行回退逻辑：

```python
# 回退到 SMART_LLM
except Exception as e:
    # ...
    response = await create_chat_completion(
        model=cfg.smart_llm_model,
        # ...
    )
```

`FAST_LLM` 在此环节**不被使用**，因为它通常被保留用于对性能要求高、但对推理深度要求较低的任务。生成研究计划被认为是一项需要深度思考的战略性任务。

### 2. `max_iterations` 的默认值是多少？

**答案：** 如果您未进行任何设置，`max_iterations` 的**默认值是 `3`**。

**代码依据：**
同样在 `generate_sub_queries` 函数中，构建 Prompt 时可以看到如下代码：

```python
gen_queries_prompt = prompt_family.generate_search_queries_prompt(
    # ...
    max_iterations=cfg.max_iterations or 3,
    # ...
)
```

`cfg.max_iterations or 3` 的逻辑是：首先尝试从配置对象 `cfg` 中读取 `max_iterations`。如果该值不存在（为 `None`），则 Python 的 `or` 运算符会返回其后的值，即 `3`。因此，框架默认会生成 3 个子查询来探索研究主题。

---

## 第二部分：上下文处理的深入思考与改进方案

您提出的问题非常深刻，直指当前“全局切片+向量检索”方法的**核心缺陷**：

*   **上下文碎片化**：将所有文档无差别地切成小块，破坏了单篇文档内部的论证逻辑和上下文联系。
*   **来源归因困难**：当多个相似的文本块被合并时，LLM 很难精确地将某个观点归因于其最原始、最准确的单一来源，导致来源标注混乱。

针对您“**生成严谨、来源准确的前沿技术周报**”这一特定需求，我提出一个全新的、更注重结构和上下文完整性的处理流程。

### 改进方案核心思想：“文档级筛选，再逐篇摘要”

这个新流程将彻底改变原有的“打乱重组”模式，转而采用一种更符合人类研究习惯的“**两阶段**”策略。

#### **阶段一：文档级智能筛选 (Document-Level Filtering)**

**目标**：从80篇PDF中，快速、准确地找出与本周报主题最相关的 **Top-N** 篇完整文档（例如 N=5 或 10）。

**如何实现**：
我们不再对所有文档的全文进行切片和向量化，而是采用一种更高效的“**元信息向量化**”策略。

1.  **预处理**：在加载80篇PDF时，除了提取全文，我们还提取每篇文档的关键**元信息**，如：
    *   **标题 (Title)**
    *   **摘要 (Abstract)**，如果文档有的话。
    *   **引言和结论 (Introduction & Conclusion)**：可以提取每个PDF的前几段和最后几段作为文档核心思想的代表。

2.  **构建“文档索引”**: 我们创建一个“文档索引”列表，每个元素代表一篇文档，包含：
    *   `file_path`: 文档的路径。
    *   `metadata_text`: 由该文档的“标题 + 摘要 + 引言/结论”拼接而成的**代表性文本**。
    *   `full_text`: 该文档的完整文本内容（暂时不使用，留作后用）。

3.  **执行筛选**:
    *   将用户的**周报主题**（例如，“本周量子计算技术进展”）进行向量化。
    *   将我们创建的“文档索引”中每篇文档的 `metadata_text` 进行向量化。
    *   进行向量相似度搜索，找出与周报主题向量最相似的 **Top-N** 个 `metadata_text`。
    *   通过这种方式，我们就得到了一个包含 **N** 篇最相关文档信息的列表（包含它们的 `file_path` 和 `full_text`）。

**优势**：这个阶段非常快速，因为它只对代表性的元信息进行向量化，避免了对80篇文档全文进行昂贵的切片和向量化操作。同时，它保证了我们筛选出的结果是**完整的文档**，而不是零碎的文本块。

#### **阶段二：单篇文档摘要与结构化信息提取 (Per-Document Summarization & Extraction)**

**目标**：对上一步筛选出的每一篇相关文档，进行独立的、有深度的阅读和信息提取，并确保来源的绝对准确。

**如何实现**：
我们将遍历阶段一筛选出的N篇文档，对每一篇都执行以下操作：

1.  **构建单文档处理 Prompt**: 设计一个新的 Prompt，其指令类似于：
    > “你是一名专业的科技分析师。请仔细阅读以下这篇完整的技术文档，并根据周报主题‘{周报主题}’，完成以下任务：
    > 1.  **核心观点摘要**: 总结这篇文档提出的最核心的技术突破、发现或观点。
    > 2.  **关键数据/指标**: 提取文中的任何关键数据、性能指标或实验结果。
    > 3.  **来源信息**: 这篇文档的来源是‘{文件名}’。
    >
    > **输出格式要求**：请严格按照以下JSON格式返回结果，不要添加任何额外说明：
    > ```json
    > {
    >   "source_document": "{文件名}",
    >   "summary": "...",
    >   "key_data": [...]
    > }
    > ```”

2.  **调用 LLM**: 将这个 Prompt 和**当前这篇文档的完整文本** (`full_text`) 一起发送给 LLM（推荐使用 `SMART_LLM` 或 `STRATEGIC_LLM` 以保证理解深度）。

3.  **收集结构化信息**: 对N篇文档重复此过程，最终我们会得到一个列表，其中包含了 **N** 个结构化的JSON对象。每个对象都是对一篇相关文档的深度解读，并且**来源信息被精确地绑定在了一起**。

#### **阶段三：整合为最终上下文**

**目标**：将上一步生成的、结构化的信息块整合成最终的、有序的上下文，供报告生成器使用。

**如何实现**：
这一步非常简单。我们只需要将阶段二得到的JSON对象列表进行序列化（例如，转换成一个大的JSON字符串或格式化的Markdown文本），就得到了最终的上下文。

**示例最终上下文**:
```markdown
**来源文档: paper_A.pdf**
- **摘要**: 本文提出了一种新的量子纠错码，将退相干时间延长了两个数量级。
- **关键数据**: 错误率从 10^-3 降低到 10^-5。

---

**来源文档: article_B.pdf**
- **摘要**: 谷歌的研究团队通过“悬铃木”处理器演示了超越经典计算机的量子霸权。
- **关键数据**: 完成特定计算任务仅需200秒，而传统超算需要一万年。

...
```

### 新方案的优势

1.  **上下文完整性**: LLM 在处理每篇文档时，都能看到其**完整内容**，从而能准确理解其内部逻辑和论点，避免了因文本切片导致的“断章取义”。
2.  **来源绝对准确**: 信息提取和来源标注是在**单篇文档内部**完成并绑定的。摘要和其来源是“原子操作”，绝不会出现将A文档的观点错误地标注为B文档的情况。
3.  **结构化与高质量**: 提供给最终报告生成器的不再是混乱的文本片段，而是**结构清晰、经过提炼的信息单元**。这极大地降低了报告生成器的工作难度，使其能更专注于“写作”和“组织”，从而生成逻辑更严谨、内容更可信的报告。
4.  **效率与成本**: 在文档数量可控（如80篇）的情况下，这种方法可能比对所有文档进行全文切片和向量化更高效，因为它将昂贵的LLM调用限制在了少数几篇最相关的文档上。

这个改进方案将从根本上解决您所担心的问题，使框架能够生成真正严谨、可靠、来源清晰的技术周报。
